<?xml version="1.0" encoding="UTF-8"?>
<charter version="1.0" name="kellylaura-site" status="v1-scoped">

  <metadata>
    <project>kellylauraart.com</project>
    <owner>James (development), Kelly (artist / merchant)</owner>
    <type>E-commerce + portfolio</type>
    <target_launch_window>6 weeks from kickoff</target_launch_window>
    <repository>github.com/[owner]/kellylaura-web</repository>
    <hosting>Vercel</hosting>
  </metadata>

  <purpose>
    <summary>
      A single, unified-brand e-commerce and portfolio site for Kelly Laura, a
      landscape painter and vintage clothing curator. The site sells original
      paintings and one-of-a-kind vintage clothing directly, accepts commission
      inquiries, and presents her work with the visual seriousness of a small
      gallery website.
    </summary>
    <success_criteria>
      <criterion>Kelly can independently publish a new painting or vintage item via Sanity Studio in under 5 minutes, without developer involvement.</criterion>
      <criterion>A customer can complete a purchase (original painting OR vintage item) end-to-end on mobile in under 3 minutes, including shipping address entry.</criterion>
      <criterion>Each product is unique inventory (qty=1); the site never sells the same physical object twice.</criterion>
      <criterion>Commission inquiries arrive in Kelly's inbox as structured email within 60 seconds of submission.</criterion>
      <criterion>Lighthouse mobile performance score >= 90 on home, gallery, and product detail pages.</criterion>
      <criterion>All product imagery is served via next/image with appropriate sizes; no raw img tags in the codebase.</criterion>
    </success_criteria>
    <explicitly_out_of_scope>
      <item>Customer accounts / login</item>
      <item>Wishlists, favorites, reviews</item>
      <item>Prints, reproductions, or product variants (each item is one-of-a-kind)</item>
      <item>Multi-currency or international tax calculation beyond Stripe defaults</item>
      <item>Blog / journal / studio diary (deferred to v1.1)</item>
      <item>Newsletter signup wired to ESP (capture email to Sanity in v1, integrate later)</item>
      <item>Search (browse is sufficient at launch inventory volume)</item>
      <item>Returns automation (manual process, contact form sufficient)</item>
    </explicitly_out_of_scope>
  </purpose>

  <stack>
    <framework>
      <name>Next.js</name>
      <version>15.x</version>
      <router>App Router</router>
      <language>TypeScript (strict)</language>
      <rendering>Server Components by default; Client Components only where interactivity requires</rendering>
    </framework>

    <styling>
      <css>Tailwind CSS v4</css>
      <components>shadcn/ui (selective; do not adopt wholesale)</components>
      <fonts>
        <display>Bodoni Moda (weights 400–800, regular + italic), via next/font/google. High-contrast Didone serif — dramatic hairline strokes, elegant italic. The actual font family of the reference design ("MZIA" / "Our Fragrances" / "Leather &amp; Patchouli").</display>
        <body>Bodoni Moda for paragraph copy. Inter Tight is retained behind --font-ui for tappable controls (.text-ui — buttons, form submits) where Bodoni's hairlines disappear at small sizes.</body>
      </fonts>
    </styling>

    <cms>
      <name>Sanity</name>
      <hosting>Sanity-hosted (Growth plan or below)</hosting>
      <studio_location>/studio route, mounted in the same Next.js app</studio_location>
      <dataset>production (and development for local work)</dataset>
    </cms>

    <commerce>
      <payments>Stripe Checkout (hosted, not Elements) for v1</payments>
      <reasoning>
        Hosted Checkout removes PCI scope, gives us Apple Pay / Google Pay / Link
        with no extra work, and is the right default for a low-volume, high-AOV
        store. Migrate to Elements later only if checkout customization becomes
        a real constraint.
      </reasoning>
      <inventory_model>
        Each product is qty=1, one-of-a-kind. Inventory state lives in Sanity
        (status field: available | reserved | sold). Stripe is payment only,
        not inventory.
      </inventory_model>
    </commerce>

    <email>
      <transactional>Resend</transactional>
      <templating>React Email</templating>
      <flows>
        <flow>Order confirmation to customer</flow>
        <flow>Order notification to Kelly</flow>
        <flow>Commission inquiry to Kelly</flow>
      </flows>
    </email>

    <observability>
      <analytics>Vercel Analytics + Vercel Speed Insights</analytics>
      <error_tracking>Sentry (free tier)</error_tracking>
      <logging>Vercel runtime logs; structured console.log with request IDs</logging>
    </observability>

    <forbidden>
      <item reason="Conflicts with stack decision">Shopify, BigCommerce, Snipcart, any commerce platform other than Stripe</item>
      <item reason="Generic AI aesthetic">Inter, Roboto, or system fonts as the primary BODY face. Inter Tight is reserved for caps-tracked UI labels on tappable controls only (.text-ui on buttons / form submits); do not use it for paragraph copy, section labels, or nav.</item>
      <item reason="Breaks the scrapbook column">Wide layouts (max-w-screen-2xl, w-full sections). The site reads as a narrow archival column even on desktop — Container max widths cap at ~40rem / 64rem.</item>
      <item reason="Bundle bloat">Material UI, Chakra, Ant Design, Bootstrap</item>
      <item reason="Unnecessary at this scale">Redux, Zustand, MobX (React Server Components + URL state + form actions are sufficient)</item>
      <item reason="Wrong primitive">localStorage / sessionStorage for cart state (use cookies via server actions)</item>
    </forbidden>
  </stack>

  <information_architecture>
    <route path="/" name="Home">
      <purpose>Brand introduction, hero painting, two-section split into Paintings and Vintage, featured works strip, about teaser, newsletter capture.</purpose>
      <data>Sanity: featured paintings (4), hero work (1), about-strip excerpt</data>
    </route>

    <route path="/paintings" name="Paintings Index">
      <purpose>Browse all available paintings. Filterable by status (available / sold) and series/tag.</purpose>
      <data>Sanity: all paintings, sorted by createdAt desc by default</data>
      <ui>Asymmetric editorial grid; varied aspect ratios respected; sold pieces are visible but visually distinct (subtle overlay + "Sold" label).</ui>
    </route>

    <route path="/paintings/[slug]" name="Painting Detail">
      <purpose>Single painting product page. Primary purchase surface for originals.</purpose>
      <data>Sanity: full painting record; related works (3, by tag overlap)</data>
      <elements>
        <element>Full-bleed primary image with secondary detail images in a vertical strip</element>
        <element>Title, year, medium, dimensions, price</element>
        <element>Artist statement / story (optional rich text)</element>
        <element>Shipping &amp; care expandable section (shared component)</element>
        <element>Buy button -> Stripe Checkout (sold-out state if unavailable)</element>
        <element>Related works strip</element>
      </elements>
    </route>

    <route path="/vintage" name="Vintage Index">
      <purpose>Browse vintage clothing. Filterable by category (dresses, outerwear, knitwear, etc.), size, and era.</purpose>
      <data>Sanity: all vintage items, available first</data>
    </route>

    <route path="/vintage/[slug]" name="Vintage Detail">
      <purpose>Single vintage item product page.</purpose>
      <elements>
        <element>Multiple photos including condition close-ups and measurement reference</element>
        <element>Era, size (with flat measurements, not just label size), material, condition notes</element>
        <element>Price, buy button</element>
        <element>"Notes on vintage sizing" link (shared content)</element>
      </elements>
    </route>

    <route path="/commissions" name="Commissions">
      <purpose>Explain Kelly's commission process; submit an inquiry.</purpose>
      <elements>
        <element>Process explanation (3-5 steps, prose)</element>
        <element>Typical timeline and price ranges (not a price list)</element>
        <element>Gallery of past commissions (Sanity)</element>
        <element>Inquiry form: name, email, project type, budget range, timeline, message, optional reference image upload</element>
      </elements>
    </route>

    <route path="/studio" name="About / Studio">
      <purpose>Kelly's story, practice, location, influences. Editorial long-form.</purpose>
      <data>Sanity: about page (singleton)</data>
    </route>

    <route path="/checkout/success" name="Checkout Success">
      <purpose>Post-purchase confirmation; triggered by Stripe redirect.</purpose>
      <behavior>Verifies session via Stripe API; updates Sanity item status to "sold"; sends email via Resend.</behavior>
    </route>

    <route path="/checkout/cancel" name="Checkout Cancel">
      <purpose>Graceful return after abandoned checkout. Restores reservation state if applicable.</purpose>
    </route>

    <route path="/api/checkout" name="Checkout Session API" type="route_handler">
      <purpose>Creates Stripe Checkout session; reserves inventory in Sanity.</purpose>
    </route>

    <route path="/api/webhooks/stripe" name="Stripe Webhook" type="route_handler">
      <purpose>Authoritative inventory state update. Handles checkout.session.completed and checkout.session.expired.</purpose>
    </route>

    <route path="/api/commission-inquiry" name="Commission Form Handler" type="route_handler">
      <purpose>Validates inquiry; sends email via Resend; stores submission in Sanity.</purpose>
    </route>

    <route path="/studio-admin/[[...tool]]" name="Sanity Studio">
      <purpose>Mounted Sanity Studio for Kelly's content editing.</purpose>
      <auth>Sanity's own auth; no app-level auth required</auth>
    </route>

    <route path="/legal/shipping" name="Shipping Policy" />
    <route path="/legal/returns" name="Returns Policy" />
    <route path="/legal/privacy" name="Privacy Policy" />
    <route path="/legal/terms" name="Terms" />
  </information_architecture>

  <data_model>
    <sanity_schemas>

      <schema name="painting" type="document">
        <field name="title" type="string" required="true" />
        <field name="slug" type="slug" source="title" required="true" />
        <field name="year" type="number" required="true" />
        <field name="medium" type="string" description="e.g., Oil on linen, Soft pastel on sanded paper" required="true" />
        <field name="dimensions" type="object">
          <subfield name="widthInches" type="number" required="true" />
          <subfield name="heightInches" type="number" required="true" />
          <subfield name="depthInches" type="number" />
          <subfield name="framed" type="boolean" />
        </field>
        <field name="weightOz" type="number" description="For shipping cost calculation" />
        <field name="price" type="number" required="true" description="USD, integer dollars" />
        <field name="status" type="string" required="true" options="available|reserved|sold|nfs" default="available" />
        <field name="reservedUntil" type="datetime" description="Set when checkout session opens; cleared on completion or expiry" />
        <field name="stripeSessionId" type="string" description="Active checkout session, if any" />
        <field name="primaryImage" type="image" required="true" hotspot="true" />
        <field name="detailImages" type="array" of="image" />
        <field name="story" type="blockContent" description="Optional artist statement" />
        <field name="tags" type="array" of="reference -> tag" />
        <field name="series" type="reference -> series" />
        <field name="featured" type="boolean" default="false" />
        <field name="featuredOrder" type="number" />
        <field name="createdAt" type="datetime" required="true" />
        <field name="soldAt" type="datetime" />
        <field name="shippingNotes" type="text" description="Optional per-piece shipping notes (oversized, etc.)" />
      </schema>

      <schema name="vintageItem" type="document">
        <field name="title" type="string" required="true" />
        <field name="slug" type="slug" source="title" required="true" />
        <field name="category" type="string" required="true" options="dress|top|bottom|outerwear|knitwear|accessory|other" />
        <field name="era" type="string" options="pre-1960|1960s|1970s|1980s|1990s|2000s|unknown" />
        <field name="labelSize" type="string" description="As marked on garment, may be empty" />
        <field name="measurements" type="object">
          <subfield name="chestFlat" type="number" />
          <subfield name="waistFlat" type="number" />
          <subfield name="hipFlat" type="number" />
          <subfield name="length" type="number" />
          <subfield name="sleeve" type="number" />
          <subfield name="shoulder" type="number" />
          <subfield name="notes" type="text" />
        </field>
        <field name="material" type="string" />
        <field name="condition" type="string" required="true" options="excellent|very-good|good|fair" />
        <field name="conditionNotes" type="text" />
        <field name="price" type="number" required="true" />
        <field name="status" type="string" required="true" options="available|reserved|sold" default="available" />
        <field name="reservedUntil" type="datetime" />
        <field name="stripeSessionId" type="string" />
        <field name="primaryImage" type="image" required="true" hotspot="true" />
        <field name="detailImages" type="array" of="image" />
        <field name="description" type="blockContent" />
        <field name="weightOz" type="number" required="true" />
        <field name="createdAt" type="datetime" required="true" />
        <field name="soldAt" type="datetime" />
      </schema>

      <schema name="series" type="document">
        <field name="title" type="string" required="true" />
        <field name="slug" type="slug" />
        <field name="description" type="text" />
      </schema>

      <schema name="tag" type="document">
        <field name="title" type="string" required="true" />
        <field name="slug" type="slug" />
      </schema>

      <schema name="commissionInquiry" type="document">
        <field name="submittedAt" type="datetime" required="true" />
        <field name="name" type="string" required="true" />
        <field name="email" type="string" required="true" />
        <field name="projectType" type="string" />
        <field name="budgetRange" type="string" />
        <field name="timeline" type="string" />
        <field name="message" type="text" required="true" />
        <field name="referenceImages" type="array" of="image" />
        <field name="status" type="string" options="new|responded|in-progress|completed|declined" default="new" />
        <field name="internalNotes" type="text" />
      </schema>

      <schema name="commissionExample" type="document" description="Past commission for the public gallery">
        <field name="title" type="string" required="true" />
        <field name="image" type="image" required="true" />
        <field name="story" type="text" />
        <field name="completedYear" type="number" />
      </schema>

      <schema name="aboutPage" type="document" singleton="true">
        <field name="hero" type="image" />
        <field name="story" type="blockContent" required="true" />
        <field name="pullQuote" type="text" />
        <field name="studioImages" type="array" of="image" />
      </schema>

      <schema name="siteSettings" type="document" singleton="true">
        <field name="featuredHeroPainting" type="reference -> painting" />
        <field name="marqueePhrases" type="array" of="string" />
        <field name="shippingPolicy" type="blockContent" />
        <field name="returnsPolicy" type="blockContent" />
        <field name="privacyPolicy" type="blockContent" />
        <field name="terms" type="blockContent" />
        <field name="instagramHandle" type="string" />
        <field name="contactEmail" type="string" required="true" />
        <field name="shippingFlatRateUS" type="number" default="45" />
        <field name="shippingFlatRateIntl" type="number" default="120" />
      </schema>

      <schema name="newsletterSignup" type="document">
        <field name="email" type="string" required="true" />
        <field name="submittedAt" type="datetime" required="true" />
        <field name="source" type="string" />
      </schema>

    </sanity_schemas>
  </data_model>

  <inventory_and_checkout_flow>
    <description>
      Critical: every product is one-of-a-kind. The race condition between two
      customers initiating checkout simultaneously is the single most important
      correctness concern in this system. Spec follows.
    </description>

    <step n="1" name="Initiate Checkout">
      <trigger>Customer clicks Buy on a product detail page.</trigger>
      <action>
        POST /api/checkout with productType + productId. Server reads current
        product status from Sanity. If status != "available", return 409. If
        available, atomically set status="reserved", reservedUntil=now+30min,
        stripeSessionId=null (pending), and only then create the Stripe Checkout
        session with metadata { productType, productId, sanityRevisionId }.
        Persist sessionId back to Sanity. Return Stripe Checkout URL.
      </action>
      <atomicity_note>
        Use Sanity's optimistic concurrency via the patch().ifRevisionId() API.
        If the patch fails because another request beat us, return 409 to the
        customer with a "just sold" message.
      </atomicity_note>
    </step>

    <step n="2" name="Customer Pays at Stripe">
      <trigger>Customer completes Stripe-hosted Checkout.</trigger>
      <action>Stripe redirects to /checkout/success?session_id=...</action>
    </step>

    <step n="3" name="Webhook Confirms Sale">
      <trigger>Stripe sends checkout.session.completed to /api/webhooks/stripe.</trigger>
      <action>
        Verify webhook signature. Look up product by sessionId in Sanity.
        Set status="sold", soldAt=now, clear reservedUntil and stripeSessionId.
        Trigger Resend emails (customer confirmation, Kelly notification).
      </action>
      <idempotency>
        Webhook handler must be idempotent. Stripe retries on non-2xx. If product
        is already "sold" with matching sessionId, return 200 without changes.
      </idempotency>
    </step>

    <step n="4" name="Expiry / Cancel">
      <trigger>Stripe sends checkout.session.expired, OR scheduled cron runs.</trigger>
      <action>
        Find products where reservedUntil < now AND status = "reserved".
        Reset status to "available", clear reservation fields.
      </action>
      <cron>
        Vercel Cron at /api/cron/release-stale-reservations every 10 minutes.
        Defense in depth; webhook is primary.
      </cron>
    </step>

    <step n="5" name="Success Page UX">
      <action>
        /checkout/success verifies session via Stripe API server-side. Does NOT
        update inventory (webhook owns that). Renders confirmation with order
        summary. Safe to refresh.
      </action>
    </step>
  </inventory_and_checkout_flow>

  <design_system>
    <surface_mode>
      Archival scrapbook. Narrow centred dark editorial column with warm
      beige typography and borders. Decided 2026-05-31 (cream/charcoal
      Cormorant pivot) and refined further the same day to the current
      "luxury perfume brand's archival scrapbook" brief — minimal,
      romantic, slightly gothic, quiet, expensive. Token names preserved
      from the original light palette so existing utilities compile; what
      changed is the colour values + role mapping:
        • ink / ink-soft       → SURFACES (warm near-black page + faint card lift)
        • bone / bone-deep     → TEXT &amp; BORDERS (warm beige primary, aged secondary)
        • ochre / ochre-deep   → background-only accents (flat beige button face)
        • rule                 → warm beige hairlines on dark
    </surface_mode>

    <tokens>
      <color name="ink" value="#0a0908" role="warm near-black page background" />
      <color name="ink-soft" value="#131110" role="faint card lift / elevated section" />
      <color name="bone" value="#d4c5a0" role="primary warm beige — text AND borders" />
      <color name="bone-deep" value="#8a7d63" role="aged beige — secondary text, tag labels" />
      <color name="ochre" value="#d4c5a0" role="alias of bone — backgrounds only, never text" />
      <color name="ochre-deep" value="#b8a984" role="deeper beige — flat-button face on hover" />
      <color name="rule" value="#4a4030" role="warm beige hairline border on dark — card frames, dividers" />
      <color name="sold-overlay" value="rgba(10, 9, 8, 0.65)" role="sold-state veil over product image" />
    </tokens>

    <colour_rules>
      <rule priority="firm">
        Two colours only — warm beige on warm near-black. There are no
        accent hues. The ochre tokens are aliases of beige for backwards
        compatibility and survive only as backgrounds (flat button face).
        Never `text-ochre*` or `decoration-ochre`.
      </rule>
      <rule priority="firm">
        Borders are beige (bone), not subtle grey. Card frames use
        `border border-rule` (warm beige hairline) and lift to
        `border-bone` on hover.
      </rule>
      <rule priority="firm">
        Buttons are flat beige rectangles: `bg-bone text-ink` solid or
        `border border-bone text-bone` outline. No rounded corners, no
        gradients, no shadows. Hover dims the fill to bone-deep.
      </rule>
    </colour_rules>

    <typography>
      <scale base="16px">
        <step name="display-xl" size="clamp(2.75rem, 8vw, 5rem)" family="Bodoni Moda" weight="400" tracking="0em" usage="Largest display — sparing, e.g. featured-plate caps title (MZIA-style)." />
        <step name="display-lg" size="clamp(2.25rem, 6vw, 3.75rem)" family="Bodoni Moda" weight="400" tracking="0em" />
        <step name="display-md" size="clamp(1.75rem, 4vw, 2.5rem)" family="Bodoni Moda" weight="400" tracking="0em" usage="Section headings (Our Rooms, Letters from the studio)." />
        <step name="body-lg" size="1.05rem" family="Bodoni Moda" weight="400" leading="1.65" />
        <step name="body" size="1rem" family="Bodoni Moda" weight="400" leading="1.6" />
        <step name="meta" size="0.88rem" family="Bodoni Moda" weight="400" style="italic" usage=".text-meta — small italic body for caption / metadata strings (medium, size, year, count, price)." />
        <step name="roman" size="0.9rem" family="Bodoni Moda" weight="500" tracking="0.18em" transform="uppercase" usage=".text-roman — Bodoni small-caps for Roman numerals (I, II, III, …, XII) on plate cards." />
        <step name="tag" size="0.62rem" family="Inter Tight" weight="500" tracking="0.24em" transform="uppercase" usage=".text-tag — tiny caps-tracked sans for the archival tags (vol. i, no. 01, est. 2018, kelly laura, explore, elsewhere). The dominant small-label utility on the site." />
        <step name="subtle" size="0.72rem" family="Bodoni Moda" weight="400" style="italic" usage=".text-subtle — tiny italic subtitle directly beneath a plate title (eau-de-toilette voice)." />
        <step name="ui" size="0.7rem" family="Inter Tight" weight="500" tracking="0.2em" transform="uppercase" usage=".text-ui — reserved for actual buttons / form submit only. Not for labels or section markers." />
        <step name="caption" size="0.85rem" family="Bodoni Moda" weight="400" style="italic" />
      </scale>
      <pairing_rule>
        Italic Bodoni Moda is the dominant voice — page headings, plate
        titles, section labels, captions, nav links, footer voice line.
        Caps Bodoni is reserved for big featured-plate titles (the
        "MZIA"-style centred caps inside the hero card) and Roman
        numerals. Inter Tight appears only in two utilities: `.text-tag`
        (the tiny archival labels — vol. i, no. 01, explore, elsewhere)
        and `.text-ui` (the flat beige buttons). Body copy and section
        markers never use Inter Tight.
      </pairing_rule>
      <case_voice>
        Lowercase italic Bodoni is the everyday gallery voice. Caps
        Bodoni for distinctive plate names. Caps tracked Inter Tight
        for archival tags. The combination of italic display serif +
        tiny caps tag is the perfume-bottle signature of the design.
      </case_voice>
    </typography>

    <spatial>
      <column>
        Narrow centred column on every viewport. Container max widths:
        narrow=34rem, default=40rem, wide=64rem, bleed=full. "default"
        is the page column and is what Nav, Footer, page sections, and
        the doorway grid all use. The whole site reads as a single tight
        magazine column, not a responsive web app.
      </column>
      <grid>2-column inside the column. The doorway grid, the satellite
        plate row, and product grids are all 2-col with small gaps
        (gap-3 mobile, gap-4 desktop). No 3+ column grids.</grid>
      <section_padding>~3.5rem mobile / ~5rem desktop between sections.
        Tighter than a typical web layout — scrapbook pages are dense.</section_padding>
      <philosophy>
        Asymmetric compositions inside the narrow column: archival
        masthead with rule under it, featured hero plate, two satellite
        plates (one solid, one outline) for paired-but-uneven feel,
        centred section header, then a 2-col plate grid. The reference
        (documentation/samples/Screenshot.png) is the source of truth
        for which sections appear in what order.
      </philosophy>
      <card_anatomy>
        Every product / doorway / satellite tile is a FRAMED PLATE with
        the same anatomy:

          [ border border-rule + p-3 ]
          [ tag row: text-tag (no. 01) ……… text-roman (I) ]
          [ square image (aspect-square, overflow-hidden, bg-ink) ]
          [ hr.border-rule (the archival hairline) ]
          [ text-center caption: italic title + text-tag subtitle ]

        The frame hovers from border-rule (faint) to border-bone
        (visible) on hover. Canonical implementation:
        components/site/CategoryDoorways.tsx → DoorwayPlate.
        PaintingCard and VintageCard follow the same shape.
      </card_anatomy>
      <featured_hero>
        The home page opens with a single large FeaturedHero plate
        (aspect-[4/5] image) followed by a hairline and a left-aligned
        caps Bodoni title with a Roman numeral on the right — the
        "MZIA" composition from the reference. See app/page.tsx.
      </featured_hero>
    </spatial>

    <motion>
      <principle>Restraint. One well-orchestrated entrance per section beats five micro-interactions.</principle>
      <hero_entrance>Staggered fade-up on scroll-into-view via the Reveal component (IntersectionObserver). 80ms increments between siblings, cubic-bezier(0.16, 0.84, 0.32, 1).</hero_entrance>
      <hover>Image scale 1.03–1.04 on 900–1100ms ease inside plates; plate frames shift from border-rule to border-bone on hover.</hover>
      <forbidden>Bouncy springs, page-load spinners on Server Component routes, parallax scrolling, scroll-jacking, colour-flashing hovers (no orange/ochre shift on hover).</forbidden>
    </motion>

    <reference>
      Visual direction is set by documentation/samples/Screenshot.png — a
      narrow, centred, dark editorial scrapbook page. Near-black bg, warm
      beige type and borders, asymmetrical grids, vintage botanical imagery,
      old-photo textures, Roman numerals, tiny italic labels, flat beige
      buttons. Treat it as the source of truth. If a screen does not feel
      like a luxury perfume brand's archival scrapbook — minimal, romantic,
      slightly gothic, quiet, expensive — the screen is wrong, not the brief.
      All earlier mockups (Galleria, bone/light surface, dense uniform grid)
      are superseded.
    </reference>
  </design_system>

  <integrations>
    <integration name="Sanity">
      <env>NEXT_PUBLIC_SANITY_PROJECT_ID, NEXT_PUBLIC_SANITY_DATASET, SANITY_API_READ_TOKEN, SANITY_API_WRITE_TOKEN</env>
      <client>@sanity/client; use next-sanity helpers; cache fetches with Next.js cache tags</client>
      <revalidation>On-demand revalidation via Sanity webhook -> /api/revalidate. Tag-based: paintings, vintage, about, settings.</revalidation>
    </integration>

    <integration name="Stripe">
      <env>STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET</env>
      <mode>Checkout Sessions (hosted)</mode>
      <products>Created dynamically per session; do not pre-create Stripe products for one-off inventory</products>
      <shipping>Use Stripe's shipping address collection; flat-rate options pulled from Sanity siteSettings</shipping>
      <tax>Stripe Tax enabled; Kelly registers in Indiana initially</tax>
    </integration>

    <integration name="Resend">
      <env>RESEND_API_KEY, RESEND_FROM_ADDRESS</env>
      <templates>OrderConfirmation, OrderNotification (to Kelly), CommissionInquiry (to Kelly)</templates>
    </integration>

    <integration name="Vercel">
      <env>VERCEL_URL handled automatically; set SITE_URL for production</env>
      <crons>/api/cron/release-stale-reservations every 10 minutes</crons>
    </integration>
  </integrations>

  <build_phases>

    <phase number="0" name="Foundation" duration="3 days">
      <goal>Project scaffolded, deployed, fonts and tokens loading, empty pages routable.</goal>
      <tasks>
        <task>create-next-app with TypeScript, Tailwind v4, App Router</task>
        <task>Configure ESLint + Prettier + strict TS settings</task>
        <task>Set up Vercel project, connect repo, configure preview deploys</task>
        <task>Add design tokens to globals.css via @theme directive (Tailwind v4)</task>
        <task>Wire next/font for Bodoni Moda (display + body) and Inter Tight (UI labels only)</task>
        <task>Scaffold all routes from information_architecture as empty pages</task>
        <task>Build shared Nav and Footer components matching the mockup</task>
      </tasks>
      <exit_criteria>Production deploy at temporary Vercel URL loads with correct typography and palette on every route, even if pages are stubs.</exit_criteria>
    </phase>

    <phase number="1" name="Sanity + Schemas" duration="3 days">
      <goal>Sanity project live, all schemas defined, Studio mounted, sample content seeded.</goal>
      <tasks>
        <task>Create Sanity project; configure CORS for local + production origins</task>
        <task>Implement all schemas from data_model section</task>
        <task>Mount Studio at /studio-admin route</task>
        <task>Write GROQ queries for: paintings list, painting by slug, vintage list, vintage by slug, featured paintings, site settings, about page</task>
        <task>Set up next-sanity client with cache tags and revalidation webhook</task>
        <task>Seed 3 paintings and 3 vintage items for development</task>
      </tasks>
      <exit_criteria>Kelly can log into Studio, create a painting with images, and see it appear at /paintings/[slug] on the dev deploy within 30 seconds.</exit_criteria>
    </phase>

    <phase number="2" name="Storefront Pages" duration="6 days">
      <goal>All public-facing pages built and matching mockup quality.</goal>
      <tasks>
        <task>Home page: hero, marquee, two-section split, featured works grid, about strip, newsletter capture</task>
        <task>Paintings index with filter UI (status, series, tag) via URL search params</task>
        <task>Painting detail page with image gallery, metadata, Buy button (non-functional placeholder)</task>
        <task>Vintage index and detail pages</task>
        <task>Studio/About page from singleton</task>
        <task>Legal pages (rendered from siteSettings block content)</task>
        <task>Mobile responsive pass on every page</task>
        <task>Metadata, OpenGraph, structured data (Product schema) per page</task>
      </tasks>
      <exit_criteria>Site is fully browsable. Buy buttons present but route to a "checkout coming soon" placeholder. Lighthouse mobile >= 90 on home, paintings index, painting detail.</exit_criteria>
    </phase>

    <phase number="3" name="Commerce" duration="5 days">
      <goal>End-to-end checkout works, including the inventory race condition handling.</goal>
      <tasks>
        <task>Implement /api/checkout with Sanity optimistic concurrency reservation</task>
        <task>Stripe Checkout session creation with product metadata</task>
        <task>Webhook handler at /api/webhooks/stripe with signature verification and idempotency</task>
        <task>Vercel Cron job for stale reservation cleanup</task>
        <task>Resend integration with React Email templates</task>
        <task>/checkout/success page with session verification</task>
        <task>/checkout/cancel page</task>
        <task>Sold-state UI on product cards and detail pages</task>
        <task>Test the race: open two browsers, click Buy simultaneously, verify exactly one wins</task>
      </tasks>
      <exit_criteria>A test purchase using a Stripe test card completes: Sanity status flips to sold, both emails arrive, success page shows order summary. Race condition test passes.</exit_criteria>
    </phase>

    <phase number="4" name="Commissions" duration="2 days">
      <goal>Commission inquiry flow live.</goal>
      <tasks>
        <task>/commissions page with process, examples gallery, inquiry form</task>
        <task>Form with client-side validation, optional image upload to Sanity</task>
        <task>/api/commission-inquiry handler: validate, store in Sanity, email Kelly via Resend</task>
        <task>Spam protection: honeypot field + rate limiting by IP</task>
      </tasks>
      <exit_criteria>A submitted inquiry creates a Sanity record and arrives in Kelly's inbox within 60 seconds.</exit_criteria>
    </phase>

    <phase number="5" name="Pre-Launch Hardening" duration="3 days">
      <goal>Production-ready.</goal>
      <tasks>
        <task>Sentry integration for client and server errors</task>
        <task>Custom 404 and 500 pages matching brand</task>
        <task>robots.txt, sitemap.xml (dynamic from Sanity)</task>
        <task>Cookie banner if analytics requires (Vercel Analytics + Plausible may not)</task>
        <task>Accessibility audit: keyboard nav, focus states, alt text on every image, color contrast on all text</task>
        <task>Performance audit: bundle analysis, image sizes, font loading strategy</task>
        <task>Domain setup, SSL, DNS</task>
        <task>Stripe account moved from test to live mode</task>
        <task>Final content review with Kelly: copy on every page, legal text, product listings</task>
      </tasks>
      <exit_criteria>kellylauraart.com loads in production with live Stripe keys, real inventory, and Kelly can complete a real-money test transaction.</exit_criteria>
    </phase>

  </build_phases>

  <conventions>
    <code>
      <rule>Server Components are the default. Add "use client" only when state, effects, or browser APIs are required.</rule>
      <rule>Data fetching lives in /lib/sanity/queries.ts. No fetch() calls in components.</rule>
      <rule>All Sanity queries are typed via sanity-codegen or hand-written types in /types/sanity.ts.</rule>
      <rule>Route handlers return NextResponse with explicit status codes. Never throw to the framework.</rule>
      <rule>Environment variables accessed only through /lib/env.ts, validated at boot with zod.</rule>
      <rule>No client-side state libraries. URL search params for filters, server actions for forms, cookies for cart-equivalent state.</rule>
    </code>

    <git>
      <rule>Conventional commits (feat:, fix:, chore:, refactor:, docs:)</rule>
      <rule>Trunk-based; feature branches short-lived; PRs preferred for any change touching commerce or webhooks</rule>
      <rule>Never commit secrets; .env.local gitignored; production secrets only in Vercel dashboard</rule>
    </git>

    <testing>
      <philosophy>
        Test the parts that hurt when they break. Inventory race conditions and
        webhook handlers must have tests. Pixel-perfect UI tests are out of scope.
      </philosophy>
      <required_tests>
        <test>Inventory reservation: concurrent requests, only one succeeds</test>
        <test>Stripe webhook idempotency: duplicate delivery does not double-update</test>
        <test>Stale reservation cleanup: reservation older than 30min is released</test>
        <test>Commission form validation</test>
      </required_tests>
      <runner>Vitest for unit/integration; Playwright optional for one end-to-end checkout flow</runner>
    </testing>
  </conventions>

  <risks>
    <risk severity="high" name="Concurrent checkout race">
      <description>Two customers click Buy at the same moment. Without proper concurrency control, both proceed to Stripe and one will pay for a product the other has already bought.</description>
      <mitigation>Optimistic concurrency in Sanity via ifRevisionId; webhook as final authority; 30-minute reservation TTL.</mitigation>
    </risk>
    <risk severity="high" name="Webhook delivery failures">
      <description>If Stripe webhook fails to deliver, Sanity inventory stays "reserved" while the customer has paid.</description>
      <mitigation>Idempotent handler; Stripe retries automatically; defense-in-depth cron job; Sentry alert on webhook errors.</mitigation>
    </risk>
    <risk severity="medium" name="Image size on art-heavy pages">
      <description>Paintings deserve large, high-quality images, but bandwidth and Core Web Vitals push the other way.</description>
      <mitigation>next/image with appropriate sizes; AVIF/WebP via Sanity image pipeline; LCP image preloaded on detail pages.</mitigation>
    </risk>
    <risk severity="medium" name="Shipping cost variability">
      <description>Flat-rate shipping in v1 will under- or over-charge for some pieces. Large paintings cost much more to ship than small ones.</description>
      <mitigation>Per-piece shippingNotes field; Kelly can refund difference manually for v1; revisit calculated shipping in v1.1.</mitigation>
    </risk>
    <risk severity="low" name="Content lock-in to Sanity">
      <description>If Sanity changes pricing or terms, migration is non-trivial.</description>
      <mitigation>Accepted. Sanity is mature, has a stable API, and Portable Text is widely supported. Migration path exists if ever needed.</mitigation>
    </risk>
  </risks>

  <deliverables_summary>
    <item>Production deploy at kellylauraart.com</item>
    <item>Sanity Studio accessible to Kelly</item>
    <item>Working purchase flow for paintings and vintage items</item>
    <item>Working commission inquiry flow</item>
    <item>README with local setup, env variable list, deployment notes</item>
    <item>RUNBOOK with: how to add a product, how to handle a failed payment, how to release a stuck reservation, how to refund</item>
  </deliverables_summary>

</charter>
